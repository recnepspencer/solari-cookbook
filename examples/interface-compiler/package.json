{
  "name": "interface-compiler",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run test --workspace @interface-compiler/domain && npm run test --workspace @interface-compiler/worth-adapter && npm run test --workspace @interface-compiler/tool-publisher"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
